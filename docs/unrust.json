{
	"name": "unrust",
	"invocations": [
		{
			"rettype": null,
			"args": [
				{
					"type": "optional bool",
					"name": "ignorecommon"
				},
				{
					"type": "optional bool",
					"name": "droptrash"
				},
				{
					"type": "optional number",
					"name": "value"
				}
			],
			"description": "Uses rust remover on all the valid rust items found. If <i>ignorecommon</i> is given it'll avoid using on common items. If <i>droptrash</i> is given it'll drop items worthless than the <i>value</i> parameter."
		}
	]
}
